# Ethiopian NID (Fayda) OAuth2 Integration Example
# Fill these values with real credentials from Fayda (IDA)

# OAuth Client Credentials
CLIENT_ID=
REDIRECT_URI=http://localhost:3000/callback

# OAuth Provider Endpoints (Staging / Production)
AUTHORIZATION_ENDPOINT=
TOKEN_ENDPOINT=
USERINFO_ENDPOINT=

# Base64-encoded Private Key used for Client Assertion
PRIVATE_KEY=

# JWT Settings
EXPIRATION_TIME=15
ALGORITHM=RS256
CLIENT_ASSERTION_TYPE=urn:ietf:params:oauth:client-assertion-type:jwt-bearer

# Server Session Secret
SESSION_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING
